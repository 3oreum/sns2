<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.resns.post.Mapper.PostMapper">
 	<select id="selectPostList" resultType="com.resns.post.domain.Post">
 		SELECT
 			*
 		FROM
 			`post`
 	</select>
 </mapper>